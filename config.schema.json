{
  "pluginAlias": "PlayStation",
  "pluginType": "platform",
  "footerDisplay": "Homebridge plugin for PlayStation 4/5 consoles (PlayActor + external title endpoint).",

  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "endpoint": {
        "title": "Title endpoint URL",
        "type": "string",
        "required": true,
        "description": "URL of your external status endpoint (ex: http://localhost:18000/status)"
      },
      "pollInterval": {
        "title": "Poll interval (ms)",
        "type": "integer",
        "default": 15000,
        "minimum": 5000,
        "description": "Polling interval in milliseconds."
      },
      "language": {
        "title": "Language",
        "type": "string",
        "default": "en",
        "oneOf": [
          { "title": "English", "enum": ["en"] },
          { "title": "Français", "enum": ["fr"] }
        ],
        "description": "Language used for titles and log messages."
      }
    }
  }
}
